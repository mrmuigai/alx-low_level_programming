#include "main.h"

/**
 * _abs - computes the absolute value of an int
 * @n: the int in question
 *
 * Return: the abs value of the integer
 */

int _abs(int n)
{
	if (n >= 0)
		return (n);
	else
		return (-n);
}
